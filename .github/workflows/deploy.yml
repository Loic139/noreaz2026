name: Deploy to Infomaniak

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Déclencher le déploiement Infomaniak
        run: |
          curl -f -X POST "${{ secrets.INFOMANIAK_DEPLOY_HOOK }}"
